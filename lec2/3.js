function check(parametr){
    if(typeof(parametr) === null || typeof(parametr) === "undefined")
     console.log("Null or undefined");
     else
     console.log("Not Null and Not undefined");
    }
    var param;
    check(param);
    check(5);